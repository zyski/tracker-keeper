'use strict';

describe('myApp.view-task module', function() {

  beforeEach(module('myApp.view-task'));

  describe('myApp.view-task controller', function(){
/*
    it('should ....', inject(function($controller) {
      //spec body
      var ctrl = $controller('viewTaskCtrl');
      expect(ctrl).toBeDefined();
    }));
*/
  });
});